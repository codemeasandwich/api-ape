# Controller Context Test Scenarios

## Overview

Tests verifying that controller context values like `this.clientId` are accessible and unique. These scenarios ensure each connection has a unique identifier available within controllers.

## See Also

- [`../README.md`](../README.md) — Lifecycle stories overview
