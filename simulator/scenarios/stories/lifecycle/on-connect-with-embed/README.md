# On Connect With Embed Test Scenarios

## Overview

Tests verifying that embedded values set during `onConnect` are available in controller context. These scenarios ensure each client can have different embedded values accessible via `this.userId`, `this.role`, etc.

## See Also

- [`../README.md`](../README.md) — Lifecycle stories overview
