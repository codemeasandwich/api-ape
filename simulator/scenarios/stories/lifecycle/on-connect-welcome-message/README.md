# On Connect Welcome Message Test Scenarios

## Overview

Tests verifying that servers can send welcome messages to clients during the connection handshake. These scenarios ensure the `send` function in `onConnect` works correctly for single and multiple messages.

## See Also

- [`../README.md`](../README.md) — Lifecycle stories overview
