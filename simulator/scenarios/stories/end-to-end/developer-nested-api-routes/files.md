# Nested API Routes Test Scenario Files

Tests API calls at different nesting levels.

## Directory Structure

```
developer-nested-api-routes/
└── user-calls-apis-at-different-nesting-levels.js
```

## Files

### user-calls-apis-at-different-nesting-levels.js

Tests calling APIs at various depths: shallow (users), nested (users/profile), deep (nested/deep/handler), and very deep (nested/deep/very/handler).
