# Nested Routes Test Scenarios

## Overview

Tests verifying that nested directory routes resolve correctly. These scenarios ensure `index.js` files map to parent paths and deeply nested routes (3-4 levels) work as expected.

## See Also

- [`../README.md`](../README.md) — RPC stories overview
