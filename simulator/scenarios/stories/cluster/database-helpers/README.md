# Database Helpers Test Scenarios

## Overview

Tests verifying the FakeDatabase helper methods used for cluster coordination. These scenarios ensure `getState()` and `reset()` work correctly for inspecting and clearing database state during tests.

## See Also

- [`../README.md`](../README.md) — Cluster stories overview
