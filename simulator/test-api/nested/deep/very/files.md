# Very Deep Nested Controllers Module Files

This module provides the deepest nesting level (4 levels) for testing api-ape's path resolution limits.

## Directory Structure

```
very/
└── handler.js    # Maps to 'nested/deep/very/handler' endpoint
```

## Files

### `handler.js`

Maps to the `nested/deep/very/handler` endpoint. Verifies 4-level deep path resolution works correctly.
